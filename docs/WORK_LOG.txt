# Claim — Work Log
# Updated: 2026-02-27 EST

---

## 2026-02-27 EST (session 20, HK-47)

- **Server Security Audit & Fixes**: Identified 13 vulnerabilities (5 critical, 5 high, 3 medium). **ALL CRITICAL AND HIGH PRIORITY ISSUES FIXED**. Most critical: admin endpoints had NO authentication. Implemented: admin role + require_admin dependency, rate limiting on admin endpoints, HTTPS redirect, request size limits, exception handler, production validation fix, removed hardcoded credentials. Created database migration for is_admin field.
- **Authentication Security (bonus)**: Added password strength validation (min 12 chars, uppercase/lowercase/number required, common password blacklist). Added comprehensive authentication logging (failed/successful logins with IP + User-Agent). Production logs written to `logs/auth.log` for attack detection.
- **Documentation**: Created `SECURITY_AUDIT.md`, `SECURITY_FIXES_SUMMARY.md`, `AUTH_SECURITY.md`. Updated CLAUDE_HANDOFF.md and WORK_LOG.txt.
- **Files Modified**: 15 total (server models, auth, config, main, admin router, alembic migration, .gitignore, .env examples, documentation)

---

## 2026-02-25 EST (session 19, HK-47)

- **Search and Sort UI**: Added alphabetical sorting and name search to dispatch panel (market and mining destinations). Added search to solar map with auto-pan to celestial objects. All searches case-insensitive with partial matching.
- **Performance Optimizations (30-40% CPU savings)**: Added visibility checks to all UI tabs (_on_tick and _process early return when hidden). Implemented adaptive orbital updates based on game speed and map visibility (99% reduction at 1x with map hidden, every 60 ticks vs every tick). Increased dispatch popup refresh from 2s to 5s. Critical for multiplayer viability at 1x speed.
- **Documentation**: Created performance_analysis.md with detailed breakdown of all optimizations and recommendations for future work.

---

## 2026-02-25 EST (session 18, HK-47)

- **Ship Partnership System (complete)**: Leader/follower pairs travel together with shadow missions, automatic mutual aid (fuel transfer, engineer repair), combined firepower in combat with proportional damage splitting, NPC partnerships for contested asteroids, stationed ship pair dispatch, full save/load persistence, UI controls in Fleet tab. See docs/PARTNERSHIP_SYSTEM.md for full documentation.

---

## 2026-02-24 EST (session 15)

- **Clip_text fix (market_tab.gd):** `upgrade_info` and upgrade buy row `info` labels were using `clip_text = true` (overflow fix from prior session) but should wrap. Changed both to `autowrap_mode = TextServer.AUTOWRAP_WORD_SMART`.
- **Docked ships drifting from Earth (full fix):** Root cause was `is_at_earth` using live ephemeris proximity check — Earth orbits away from stored ship position. Added `docked_at_earth: bool = true` to `ship.gd`; `is_at_earth` now checks flag OR proximity. Set `true` at all 3 Earth-return points in `simulation.gd` (mining mission return, station_colony==null return path, trade mission return). Set `false` at all 4 dispatch points in `game_state.gd`. Save/load added with default `true` for backward compatibility.
- **Modular vs dry dock upgrades:** Added `requires_dry_dock: bool` field to `ShipUpgrade` model. Recategorized `upgrade_catalog.gd` — Extended Fuel Tank and Improved Thrust Nozzles are modular (physical units, buy → inventory → install); all others (fuel system restructuring, engine rebuilds, cargo bay extensions, hull work) are dry dock (structural, commissioned directly on a ship). Added `commission_dry_dock(ship, entry)` to `game_state.gd` — applies upgrade directly, no inventory step, logs as "Dry dock: [work] on [ship]". Split market tab upgrades section into "Modular Upgrades" (blue) and "Dry Dock Work" (amber) with distinct UI flows. Updated `GDD.md §8.7`.

---

## 2026-02-23 (session 14, Dweezil, time untracked)

- **Starting ships = 6 bug fixed**: `title_screen.gd` was calling `GameState._ready()` for new game, but Godot already auto-calls `_ready()` on autoload init — doubled `_init_starter_ship()` to 6 ships. Fix: added `new_game()` to `game_state.gd` that clears all state arrays/dicts, resets money/ticks, then re-runs init. `_ready()` delegates to it. `title_screen.gd` now calls `new_game()` instead. Also fixed invalid `reputation_score = 0.0` in `new_game()` → `Reputation.score = 0.0`.
- **Ship teleport after refueling fixed**: `Mission.Status.REFUELING` completion always called `_complete_refuel_stop(mission, true)` — hardcoded is_outbound=true. Return-trip fuel stops resumed wrong direction (TRANSIT_OUT using outbound_legs). Added `refueling_is_return: bool` to both `Mission` and `TradeMission`, set it in `_process_waypoint_transition`. Completion now uses `not mission.refueling_is_return`. Same fix applied to `TradeMission`.
- **Profit estimates still negative — ore price rebalance**: Previous session fixed two code bugs (wrong fuel capacity, inflated worker count). But underlying balance was still broken: worker wages ($80-200/day × 4-6 crew × 60+ day missions) overwhelmed ore revenue at Earth-commodity prices. Scaled all base ore prices ~8x: Iron $50→$400, Nickel $120→$1000, Platinum $800→$6500, Water Ice $200→$1600, Carbon $150→$1200.
- **Market tab horizontal overflow fixed**: Added `size_flags_horizontal = 3` to `ScrollContainer` node in `market_tab.tscn`, matching the pattern already applied to other tabs.

---

## 2026-02-23 (session 13, time untracked)

- Fixed ship position bug from WaypointLeg refactor: simulation completion checks used `mission.transit_time` (final leg) for all legs; fixed to use `get_current_phase_duration()` in 4 places in simulation.gd
- Fixed profit estimates all showing negative: two bugs — (1) Hohmann auto-decide used `ship.fuel` (current) not `ship.get_effective_fuel_capacity()` in asteroid_data.gd, (2) list view used all available workers inflating wages in fleet_tab.gd
- Per-ship policy overrides: added 4 override fields to Ship, resolver functions in GameState, save/load support, and policy override UI section in fleet_tab.gd
- Scroll-to-selected-ship in fleet tab: when player selects a ship on solar map then opens Fleet tab, auto-scrolls to that ship's panel
- Signal transit time live countdown: fleet_market_tab.gd now updates the "Signal in transit" label every 100ms real-time tick rather than only on rebuild

---

## 2026-02-23 (session 12, time untracked)

- Architectural review of full codebase (55 files, ~10K LOC)
- Identified 5 priority refactors:
  1. WaypointLeg class — replace 8 parallel arrays in Mission/TradeMission
  2. Split GameState into data + logic layers
  3. Accumulator helper for simulation.gd periodic events
  4. Unify Mission/TradeMission shared logic
  5. Consolidate policy getters into single parameterized function
- Updated CLAUDE_HANDOFF.md with full findings for HK-47 next session

---

## 2026-02-23 (session 11, time untracked)

- Completed per-ship policy overrides (from plan):
  - 4 fields on `ship.gd`: `thrust/supply/collection/encounter_policy_override` (default -1 = inherit company policy)
  - Resolver functions in `game_state.gd`: `get_thrust_policy(ship)`, `get_supply_policy(ship)`, `get_collection_policy(ship)`, `get_encounter_policy(ship)`
  - All direct policy reads in `simulation.gd` replaced with resolver calls
  - Save/load updated for override fields
  - "Policy Overrides" section in `fleet_tab.gd` ship detail panel with OptionButton per policy
- Removed `mission.workers` and `tm.workers` entirely from the codebase:
  - Removed `@export var workers: Array[Worker]` from `mission.gd` and `trade_mission.gd`
  - All call sites updated: crew lives on `ship.crew` only
  - Crew rotation (CREW_FERRY): uses `target_ship_rot.crew` and `ferry_ship.crew` directly
  - Deploy mission: removes deployed workers from `mission.ship.crew`
  - Food depletion: clears `ship.crew`, marks derelict
  - Rescue: assigns subset of `ferry_ship.crew` to target derelict ship
  - `test_harness.gd` validation updated to iterate `mission.ship.crew`
- Files modified: `core/models/mission.gd`, `core/models/trade_mission.gd`, `core/autoloads/simulation.gd`, `core/autoloads/game_state.gd`, `core/autoloads/test_harness.gd`, `ui/tabs/fleet_tab.gd`

---

## 2026-02-22 (session 10, time untracked)

- Mass GDScript compilation error fixing across codebase
  - Added `last_crew: Array[Worker]` to `ship.gd` (used everywhere, never declared)
  - Added `assigned_mission: Mission` and `assigned_trade_mission: TradeMission` to `worker.gd` (same)
  - Fixed type inference errors in `asteroid_data.gd`: `:= Simulation.BASE_MINING_RATE`, `:= .../Simulation.PAYROLL_INTERVAL` → explicit `: float =`
  - Removed ~15 empty `for w in ...:` loops across `simulation.gd`, `test_harness.gd` (body was never written; just deleted the dead loops)
  - Fixed ~20 wrong-argument calls where `Array[Worker]` was passed as the `transit_mode: int` argument to `start_mission`, `start_collect_mission`, `start_trade_mission`, `start_deploy_mission`, `dispatch_idle_ship`, `dispatch_idle_ship_trade`, `queue_mission` — all across `simulation.gd`, `test_harness.gd`, `fleet_market_tab.gd`, `fleet_tab.gd`, `market_tab.gd`. Fix: set `ship.crew = workers` before the call, drop the workers arg.
  - Fixed `assigned_station_ship` → `assigned_ship` in `simulation.gd`, `fleet_market_tab.gd`, `fleet_tab.gd`
  - Fixed indentation errors in `test_harness.gd` (lines displaced from their `if` blocks when crew arg was removed)
- Files modified: `core/models/ship.gd`, `core/models/worker.gd`, `core/data/asteroid_data.gd`, `core/autoloads/simulation.gd`, `core/autoloads/test_harness.gd`, `ui/tabs/fleet_market_tab.gd`, `ui/tabs/fleet_tab.gd`, `ui/tabs/market_tab.gd`

---

## 2026-02-22 (session 9, time untracked)

- Added Redispatch button for in-transit ships
  - `fleet_market_tab.gd`: added `_is_redispatch_mode` flag; added `redispatch_mode` param to `_start_dispatch`; added "Redispatch" button in in-transit ship card (before "Plan Next Mission"); updated `_show_dispatch_confirmation` to show "Redispatch Ship" title + orange warning note + "Confirm Redispatch" button that calls `_abort_and_dispatch()`
  - Redispatch aborts the current mission and immediately reroutes the ship from its current position in space

---

## 2026-02-22 (session 8, time untracked)

- Fixed teleport bug introduced in session 7
  - Root cause: session 7 removed autoplay guards from two `_policy_dispatch_idle_ship` call sites — ships auto-dispatched even with autoplay off, causing immediate departure on return to Earth
  - `simulation.gd` signal handler: added `and GameState.settings.get("autoplay", false)` guard to `_policy_dispatch_idle_ship` call
  - `simulation.gd` idle ship loop (`_process_stationed_ships`): changed `else: _policy_dispatch_idle_ship(ship)` to `elif GameState.settings.get("autoplay", false)` — queued missions still launch regardless of autoplay; policy dispatch is autoplay-only

---

## 2026-02-22 (session 7, time untracked)

- Redesigned "Plan Next Mission" to correctly sequence after all prep work
  - Root problem: `_start_queued_mission` was called inside `complete_mission`, which fires BEFORE `_auto_provision_at_location` and `_auto_repair_at_location` — ship left without being provisioned/repaired
  - `game_state.gd`: removed `_start_queued_mission` calls from both `complete_mission` and `complete_trade_mission`; removed incorrect `return_to_station = true` from queued mission launch
  - `simulation.gd`: added queued mission launch AFTER provision+repair block in transit-back completion; also handles trade mission completion; updated per-tick idle ship loop to call `_start_queued_mission` (not `_policy_dispatch_idle_ship`) when `has_queued_mission()`; updated signal handler to skip policy dispatch when queued mission exists; removed redundant early return from `_policy_dispatch_idle_ship`
  - `fleet_market_tab.gd` `_queue_mission`: fixed `get_total_ore()` / `worker.mining_rate` calls (neither exists); now uses `AsteroidData.estimate_mission()` to get proper mining duration
- Added Water and Oxygen supply types for ships (recycled — small amounts needed)
  - `supply_data.gd`: added `WATER` (tank = 20 L, 0.02 t, $40) and `OXYGEN` (canister = 2 kg O2, 0.002 t, $120); added `unit_label` field to all supply types (crate/kit/tank/canister); added `get_unit_label()` and `get_unit_label_from_key()` helpers
  - `simulation.gd` `_auto_provision_at_location`: now stocks water (0.25 L/person/day makeup) and O2 (0.05 kg/person/day makeup) alongside food
  - `fleet_market_tab.gd`: updated supplies card display to show unit labels (crates, tanks, canisters, kits); added water/O2 rows
  - `fleet_market_tab.gd` supply shop: shows unit label in headers and "on board" text
  - `game_state.gd`: updated `asteroid_supplies` default dict to include "water" and "oxygen" keys

---

## 2026-02-22 (session 6, time untracked)

- Fixed horizontal stretching in Assign Crew screen (fleet_tab + fleet_market_tab)
  - `crew_scroll.horizontal_scroll_mode = SCROLL_MODE_DISABLED` + `size_flags_horizontal = EXPAND_FILL`
  - CheckBox/Button crew rows: `clip_text = true` + `SIZE_EXPAND_FILL`
  - Long labels (destination, ore summary): `autowrap_mode = AUTOWRAP_WORD_SMART`
  - Same fix applied to deploy-unit buttons and deploy-worker buttons in fleet_market_tab
- Fixed "Plan Next Mission" feature — queued missions were ignored because `_policy_dispatch_idle_ship` ran first (via synchronous `mission_completed` signal) and assigned a policy mission, then `_start_queued_mission` tried to overwrite it, creating orphaned missions
  - `simulation.gd` `_policy_dispatch_idle_ship`: returns early if `ship.has_queued_mission()`
  - `game_state.gd` `_start_queued_mission`: now validates workers (top-up from pool if needed), checks fuel feasibility, handles COLLECT_ORE vs MINING mission types, falls back to policy on failure
  - `core/models/ship.gd`: added `queued_mission_type: int` field; updated `queue_mission()` to accept it; `clear_queued_mission()` resets it
  - `fleet_market_tab.gd` `_queue_mission()`: passes `_selected_mission_type` to `ship.queue_mission()`
  - Planning mode confirm screen: removed "Dispatch Now (Abort Current)" button (redispatch ≠ plan next); renamed to "Confirm Plan"; added green explanatory note
- Added fleet rescue cancel from derelict ship card
  - `game_state.gd`: added `find_fleet_rescue_ferry(derelict)` + `cancel_fleet_rescue(derelict)` — recalls the ferry (flips mission to TRANSIT_BACK to Earth)
  - `fleet_market_tab.gd`: derelict card now shows fleet rescue status + "Cancel Rescue" button when player's ferry is en route

---

## 2026-02-22 (session 5, time untracked)

- Fixed per-ship policy overrides UI — was implemented in dead `fleet_tab.gd`, never visible; ported to live `fleet_market_tab.gd`
- MODIFIED `ui/tabs/fleet_market_tab.gd`: added Policy Overrides section (Thrust, Supply, Collection, Encounter) to each non-derelict ship card with OptionButtons; item 0 = "Company Default" (-1), subsequent items = enum values; pre-selects current override on rebuild
- Added nebula PNG background to menus (`ui/starfield_bg.gd`): loads `StarfieldNebula1.png`, drifts slowly in randomized arc, never shows edges
- Added wrench repair icons to ship cards in fleet tab: red (0–20%), orange (21–50%), yellow (51–70%) based on worst component (engine or equipment); 20×20px, 35° tilt, shown in name row header
- MOVED `wrenches/` → `ui/wrenches/`, `starfields/` → `ui/starfields/`

---

## 2026-02-22 (session 4, time untracked)

- Food depletion now kills crew permanently instead of abandoning ship
- MODIFIED `core/autoloads/simulation.gd`: `_trigger_food_depletion()` now removes workers from `GameState.workers`, marks ship derelict (breakdown), emits `ship_derelict`
- MODIFIED `core/autoloads/event_bus.gd`: renamed `ship_food_depleted` param from `workers_abandoned` to `workers_killed`
- MODIFIED `core/data/ship_data.gd`: added 11 ship names (pool now 30): Modest Ambitions, Known Issue, Salt and Tarnish, Norfleet, Borrowed Light, Belligerent Optimism, Cimmeria, Technically Profitable, Song of Many, Sing Forever, Of Distant Suns
- Removed `docs/WORK_LOG.txt` from git tracking (`git rm --cached`); already covered by `/docs/` gitignore

---

## 2026-02-22 (session 3, time untracked)

- Implemented fleet assist / crew rescue system for derelict fleet ships
- MODIFIED `core/models/mission.gd`: added `is_derelict_rescue: bool`, `rescue_crew: Array[Worker]`, `supplies_to_transfer: Dictionary` to Mission resource for derelict takeover missions
- MODIFIED `core/autoloads/game_state.gd`: added `start_fleet_rescue(ferry_ship, target_ship, rescue_crew, food_units, parts_units) -> Mission` — builds combined crew, creates CREW_FERRY mission targeting derelict position, deducts supplies from ferry upfront
- MODIFIED `core/autoloads/simulation.gd`: extended `_complete_boarding_job()` to branch on `is_derelict_rescue`: transfers supplies, applies -20 loyalty penalty to rescue crew, sets target ship's `last_crew`, clears derelict status, erases from rescue/refuel/stranger_offers dicts, emits rescue_mission_completed, calls `_start_station_return`; renamed standard rotation variable to `target_ship_rot` to avoid conflict
- MODIFIED `ui/tabs/fleet_market_tab.gd`:
  - Fixed cargo display: supplies are now shown in a separate "Supplies (Xt):" label with food-days estimate, not merged under "Cargo"
  - Ore label now reads "Ore (Xt):" to distinguish from supplies
  - Added "Supplies" button to all docked (non-stationed) ships for supply management
  - Removed redundant stationed-ship supply text label (replaced by global supplies section)
  - Added "FLEET SHIPS NEEDING HELP" section at top of dispatch popup listing derelict fleet ships with transit time + fuel check
  - Added `_show_fleet_rescue_dispatch(target_ship)` — shows transit info, supply spinboxes (food + parts to send), worker checkboxes (rescue crew who stay on target ship), dispatches via `GameState.start_fleet_rescue()`

---

## 2026-02-22 (session 2, time untracked)

- Implemented per-ship policy overrides for Thrust, Supply, Collection, and Encounter policies
- MODIFIED `core/models/ship.gd`: added 4 `@export` override fields (default -1 = inherit company policy): `thrust_policy_override`, `supply_policy_override`, `collection_policy_override`, `encounter_policy_override`
- MODIFIED `core/autoloads/game_state.gd`: added 4 resolver functions (`get_thrust_policy`, `get_supply_policy`, `get_collection_policy`, `get_encounter_policy`) that return ship override if ≥0, else company default; added override fields to ship save/load
- MODIFIED `core/autoloads/simulation.gd`: replaced all direct `GameState.*_policy` reads with resolver calls in `_autoplay_jobs(ship)`, `_policy_dispatch_idle_ship(ship)`, `_station_try_provisioning(ship)`, `_station_try_collect_ore(ship)`; added `ship` param to `_autoplay_jobs`
- MODIFIED `ui/tabs/fleet_tab.gd`: added "Policy Overrides" GridContainer to each non-derelict ship card — 4 OptionButtons (Thrust/Supply/Collection/Encounter), item 0 = "Company Default" (-1), remaining items = enum values from CompanyPolicy name dicts; pre-selects current override

## 2026-02-22 (session 1, time untracked)

- Implemented ghost ship observation system for rival corporations
- NEW `core/models/ghost_observation.gd`: data class with position back-calculation (light-speed corrected), velocity extrapolation, and linear confidence decay over 2 game-days
- MODIFIED `core/models/rival_ship.gd`: added `get_thrust_direction()` (brachistochrone first/second half), `get_velocity_au_per_tick()` (average speed for back-calc), `get_visibility_from(observer_pos)` (fusion exhaust dot-product visibility 0–1)
- MODIFIED `core/autoloads/game_state.gd`: added `ghost_observations: Array[GhostObservation]`
- MODIFIED `core/autoloads/simulation.gd`: added `_update_rival_observations()` — runs every 60 game-seconds, iterates rival ships, finds best player observer (Earth + all ships), applies full light-speed delay (rival→observer→HQ), back-calculates observed position, creates/replaces GhostObservation records, prunes expired ones; added CORP_COLORS array (5 colors for up to 5 corps)
- MODIFIED `solar_map/solar_map_view.gd`: added `_draw_ghost_observations()` — draws corp-colored dot (fades with confidence), uncertainty halo ring (grows as confidence drops), velocity arrow when confidence > 0.35

## 2026-02-21 ~22:30-22:45 EST

- Updated GDD.md to v0.9 reflecting this session's work
  - §3.4 Communication Delay: updated from "NOT YET IMPLEMENTED" to full implemented description (lightspeed queue, 499 s/AU, 5 order types, UI banners)
  - §3.5 Policy System: updated from "NOT YET IMPLEMENTED" to "PARTIALLY IMPLEMENTED" (all 4 policies wired, autoplay uses them; per-site overrides still deferred)
  - §5.2 Open Market: added ore selling flow documentation (auto_sell_at_earth, stockpile, colony trade)
  - §6.5 AI Corporations: updated from "NOT YET IMPLEMENTED" to full implemented description (5 named corps, personalities, hourly AI, slot competition, save/load; solar map display deferred)
- Updated CLAUDE_HANDOFF.md timestamps and session context

## 2026-02-21 ~22:00-22:30 EST

- Fixed "no deposits when selling ore" bug: root cause was that mining missions deposit ore to `GameState.resources` (stockpile) but nothing auto-sells it — autoplay re-dispatches ships immediately, so the player never has a chance to manually sell from the Market tab
- Added `auto_sell_at_earth: true` setting to `game_state.gd` — when on, ore is sold at Earth market prices immediately when a mining mission completes rather than going to stockpile
- Setting is saved/loaded alongside `autoplay` and `auto_sell_at_markets`
- Added checkbox in main_ui.gd settings panel: "Auto-sell ore when ships return to Earth" (on by default)
- Renamed "Auto-sell cargo at markets" → "Auto-sell cargo at colony markets" to distinguish the two settings

## 2026-02-21 ~21:30-22:00 EST

- Fixed ore-loss bug: trade mission ships returning to Earth with unsold cargo now have that cargo returned to the stockpile instead of being silently destroyed
- Root cause: `complete_trade_mission()` in `game_state.gd` called `tm.ship.current_cargo.clear()` without checking if `tm.cargo` still had unsold ore; ore was removed from stockpile at mission start (line 1778: `remove_resource`) but never returned
- Fix: added stockpile restoration at top of `complete_trade_mission()` — iterates `tm.cargo`, calls `add_resource()` for each ore type, then clears `tm.cargo`; the selling path clears `tm.cargo` before calling `complete_trade_mission`, so there's no double-return risk

## 2026-02-21 ~20:30-21:30 EST

- Lightspeed communication delay implemented: orders sent to remote ships are delayed by `distance × 499 s/AU` game-time
- Architecture: `pending_orders: Array[Dictionary]` in `game_state.gd`; each entry holds `fires_at`, `ship`, `label`, `fn: Callable`
- `calc_signal_delay(ship)` returns 0 for docked ships, else `distance_au * 499.0`
- `queue_ship_order(ship, label, fn)` appends to pending_orders (or executes immediately if delay = 0); emits `order_queued` signal
- `process_pending_orders()` called each tick in `simulation.gd`, fires ready orders, emits `order_executed`
- Wrapped 5 order functions in `game_state.gd` with `_apply_*` internal variants:
  - `order_return_to_earth` → queues `_apply_order_return_to_earth`
  - `redirect_mission` → queues `_apply_redirect_mission` (re-validates at arrival; realistic since ship position has changed)
  - `redirect_trade_mission` → queues `_apply_redirect_trade_mission`
  - `dispatch_idle_ship` → queues `_apply_dispatch_idle_ship`
  - `dispatch_idle_ship_trade` → queues `_apply_dispatch_idle_ship_trade`
- `event_bus.gd`: added `order_queued(ship, label, delay_secs)` and `order_executed(ship, label)` signals
- `fleet_market_tab.gd`: shows "📡 Signal in transit: <label> (<time> remaining)" banner on idle-remote and in-transit ship cards; dispatch/return buttons disabled while order is pending; redirect confirmations show "Signal delay: Xm XXs" line
- `dashboard_tab.gd`: order_queued → amber activity entry with ship name, label, and delay; order_executed → green entry confirming receipt
- Also fixed: bug fixes from prior session (cargo/supply distinction, autoplay Earth ships, ships not returning, cargo unsellable after return)

## 2026-02-21 ~19:30-20:30 EST

- Rival corporations implemented (single-player AI competition)
- Created `core/models/rival_ship.gd` — RivalShip resource with Status enum (IDLE/TRANSIT_TO/MINING/TRANSIT_HOME), cargo, thrust, transit/mining timing, `get_position_au()` for interpolated map position
- Created `core/models/rival_corp.gd` — RivalCorp resource with Personality enum (AGGRESSIVE/SYSTEMATIC/OPPORTUNISTIC/CONSERVATIVE/EXPANSIONIST), ships array, money/stats
- Created `core/data/rival_corp_data.gd` — factory for 5 named corps: Helios Extraction (AGGRESSIVE), Meridian Heavy Industries (SYSTEMATIC), Vanguard Prospecting (OPPORTUNISTIC), Consolidated Belt Resources (CONSERVATIVE), Arcturus Syndicate (EXPANSIONIST)
- `event_bus.gd`: added 4 rival signals — rival_corp_dispatched, rival_corp_arrived, rival_corp_departed, rival_corps_contested
- `game_state.gd`: added `rival_corps: Array[RivalCorp]`; initialized in `_ready()` from RivalCorpData.create_all(); updated `get_occupied_slots()` to include rival ships at MINING status; added `get_rival_occupied_slots()` and `get_player_units_at()` helpers; rival corp states serialized in save/load (stats overlay on recreated structure)
- `simulation.gd`: added _rival_accumulator/RIVAL_CHECK_INTERVAL; `_process_rival_corps(dt)` called each tick; `_advance_rival_ship()` handles per-tick transit/mining/return; `_update_rival_corp_decisions()` + `_rival_try_dispatch()` run hourly; `_score_asteroid_for_rival()` applies personality-specific scoring (AGGRESSIVE=richest, SYSTEMATIC=uncontested, OPPORTUNISTIC=follows player, CONSERVATIVE=avoids conflict, EXPANSIONIST=any open slot); `_sell_rival_cargo()` updates corp finances; `_find_asteroid()` helper
- `dashboard_tab.gd`: connected all 4 rival EventBus signals to activity/alert feed with orange color coding; contested signal shows as bold alert

## 2026-02-21 ~18:00-19:30 EST

- Ship spec cleanup: updated `ship_data.gd` CLASS_STATS to GDD S8.2 canonical values - Courier (0.38g/38t/54m3/46.5fuel/73.4t dry/crew2/slots3), Hauler (0.19g/412t/584m3/237fuel/488.2t dry/crew5/slots5), Prospector (0.31g/107t/143m3/118fuel/214.8t dry/crew3/slots4), Explorer (0.47g/63t/91m3/192fuel/141.6t dry/crew2/slots4)
- Per-ship variance updated to GDD S8.2 spreads: +-5% dry mass, +-5% thrust, +-10% cargo mass, +-10% cargo volume, +-8% fuel, +-1 slot; removed uniform +-7%
- Python server ship stats synced to match GDD in `server/server/models/ship.py`
- Policy system added: SupplyPolicy (PROACTIVE/ROUTINE/MINIMAL/MANUAL), CollectionPolicy (AGGRESSIVE/ROUTINE/PATIENT/MANUAL), EncounterPolicy (AVOID/COEXIST/CONFRONT/DEFEND) - added to `company_policy.gd` with names, descriptions, and threshold constants
- `game_state.gd`: added supply_policy, collection_policy, encounter_policy vars; wired into save/load
- `simulation.gd` _station_try_provisioning: replaced hardcoded 5-day threshold with CompanyPolicy.SUPPLY_POLICY_THRESHOLDS[GameState.supply_policy]; switched from deployed_crews to asteroid_supplies+get_asteroid_supply_days() for threshold check; skips if MANUAL
- `simulation.gd`: added _station_try_collect_ore() - auto-collects stockpiled ore when stockpile >= cargo_capacity * CollectionPolicy threshold; skips if MANUAL; added collect_ore to station job dispatch match
- `simulation.gd` _complete_delivery_job: now delivers food/parts to both deployed_crews (legacy) AND asteroid_supplies (mining units model) by proximity matching to nearest asteroid
- `dashboard_tab.gd`: replaced single thrust policy row with a generic _add_policy_row helper; now shows all 4 policies (Thrust, Resupply, Ore Collection, Encounter) each with name dropdown and live description label
- Python server skeleton created at `server/`: FastAPI + SQLAlchemy async + PostgreSQL; routers for auth/game/events/admin; simulation runs as background asyncio task (1 real second = 1 game tick at 1x); SSE event stream; seed script; Alembic migrations; full README with Windows setup instructions
- Python policy enum comments fixed in `server/server/models/player.py` to match GDScript enum values exactly

## 2026-02-21 ~17:00–17:30 EST

- Fixed redirect fuel check: previously only checked one-way fuel to new destination; ship could arrive stranded with nothing left for return trip
- Fixed redirect `fuel_per_tick`: was set to `ship.fuel / transit_time` (burning ALL remaining fuel on outbound leg, leaving 0 for return); now uses round-trip rate `(fuel_out + fuel_ret) / (outbound_time + return_time)` matching `start_mission`'s pattern
- Both `redirect_mission` and `redirect_trade_mission` now: (1) compute return fuel from new_dest back to home (Earth/station), (2) check total against ship.fuel, (3) use round-trip `fuel_per_tick`
- `redirect_cost` now based on `fuel_out` only (not total round-trip) — redirect penalty is for the course change, not the full mission
- Updated `fleet_market_tab.gd` redirect popups to use round-trip feasibility check and show correct infeasibility message

## 2026-02-21 ~16:30–17:00 EST

- Fixed mission origin display: fleet tab now shows actual origin name (e.g., "Castalia") instead of "Earth"/"Space"
- Root cause 1: `start_mission` never set `origin_is_earth` — it defaulted to `true`, so all missions started from non-Earth showed "Earth"
- Root cause 2: "Earth"/"Space" binary was never expressive enough; needed `origin_name` field
- Added `origin_name: String` to `mission.gd` and `trade_mission.gd`
- `start_mission`: now sets `origin_is_earth` from ship position + `origin_name` from Earth/colony
- `dispatch_idle_ship`: captures previous asteroid name BEFORE clearing idle mission, then sets `origin_name` on new mission (e.g., "Castalia")
- `dispatch_idle_ship_trade`: same, captures colony name
- `start_trade_mission`: now sets `origin_is_earth` + `origin_name` (was also missing)
- `start_deploy_mission` / `start_collect_mission`: already had `origin_is_earth`, added `origin_name`
- `fleet_tab.gd`: uses `origin_name` if set, falls back to "Earth"/"Space"
- Save/load: added `origin_name` to both Mission and TradeMission save/load dicts

## 2026-02-21 ~16:00–16:30 EST

- Added ship speed display to solar map: `ship_marker.gd` `_update_label()` shows `"ShipName\n{N} km/s"` for speed ≥ 0.5 km/s; speed from `thrust × 9.81 × transit_time × min(t, 1-t)` (fixed mission params, not live orbital positions which inflate due to drift); label rect height fixed in `ship_marker.tscn` (`offset_bottom` 10→28, 38px total)
- Implemented redirect momentum arc in `game_state.gd`: ships no longer snap direction instantly on redirect. Two-leg route: leg 1 continues in current velocity direction (`arc_fraction * dist`), leg 2 turns to new destination. `arc_fraction = clamp(sqrt((1-dot)/2) * speed_fraction * 0.4, 0, 0.30)`. Falls back to single-leg if arc < 5% or if ship speed too high for arc length (initial_t ≥ 0.48). Uses existing `outbound_waypoints`/`outbound_leg_times` multi-leg infrastructure.
- Velocity preservation on redirect: speed magnitude preserved via `initial_t = clamp(speed / (4 × avg_v), 0, 0.5)`, virtual origin pushed backward so position is correct at entry time. Applied to both Mission redirects and TradeMission redirects.

## 2026-02-21 ~15:00–16:00 EST

- Fixed HQ tab horizontal expansion: added SIZE_EXPAND_FILL + clip_text to labels in dashboard_tab.gd (_refresh_stationed_ships, _refresh_discipline, _refresh_resources, _refresh_alerts, _refresh_activity, _refresh_contracts, _refresh_balance_history)
- Fixed ships staying at asteroids permanently: IDLE_AT_DESTINATION ships have non-null current_mission, causing them to route into the redirect path. Fixed _on_map_dispatch_asteroid and _on_map_dispatch_colony to check specific transit statuses (TRANSIT_OUT, TRANSIT_BACK, REFUELING) instead of just if ship.current_mission
- Fixed _return_to_map_if_needed never firing: _hide_dispatch() was resetting _dispatched_from_map=false before the check; reordered calls in _execute_dispatch, _queue_mission, _select_colony_trade
- Fixed redirect fuel calculation: calculate_course_change converted AU/tick velocity to km/s producing astronomically large fuel estimates; replaced with ship.calc_fuel_for_distance(dist) in both fleet_market_tab.gd and game_state.gd
- Fixed silent failure on infeasible redirect: now always shows popup with Confirm/Cancel if feasible, or Close + reason if not
- Fixed ship position jumping back to Earth on redirect: redirect_mission and redirect_trade_mission now set origin_position_au = ship.position_au and origin_is_earth = false before resetting elapsed_ticks, preventing interpolation from restarting at Earth's current position
- Fixed ships stopping and changing direction when redirected: when a TRANSIT_BACK ship was redirected, only mission.asteroid changed but elapsed_ticks/transit_time stayed the same — the lerp start-point snapped to the new asteroid's position (potentially across the solar system). Fixed by always resetting to TRANSIT_OUT from ship's current position regardless of prior status, same fix in redirect_trade_mission
- Implemented velocity-preserving redirects: ships no longer stop and restart from rest when redirected. The brachistochrone is entered at initial_t = clamp(speed / (4 * avg_velocity), 0, 0.5) so speed is preserved. The virtual origin is pushed behind the ship (adjusted_origin = (pos - dest*dfrac)/(1-dfrac)) so position is also correct at initial_t. The popup displays the effective remaining transit time (1-initial_t)*T instead of the from-rest time

## 2026-02-21 ~14:00–15:00 EST

- Added 7 ship names to ship_data.gd: "A Searing Epiphany", "Squandered Fortune", "Laziness in Action", "Wandering Minstrel", "Faith Like a Candle", "Slurry", "Dastardly Cur"
- Confirmed orphan worker autotest error has not recurred — marked resolved in handoff doc
- Implemented Solar Map Dispatch (see previous session entry)

## 2026-02-21 ~13:00–14:00 EST

- Implemented Solar Map Dispatch feature — player can now dispatch ships spatially from the map
- event_bus.gd: Added map_dispatch_to_asteroid(ship, asteroid) and map_dispatch_to_colony(ship, colony) signals
- solar_map_view.gd: Added dispatch mode — clicking a docked ship in the left panel selects it (green border), then clicking an asteroid or colony emits dispatch signal; RMB or Escape cancels; hint label shows at bottom while in dispatch mode
- main_ui.gd: Connected map dispatch signals → auto-switches to Fleet tab (index 1)
- fleet_market_tab.gd: Added _on_map_dispatch_asteroid() and _on_map_dispatch_colony() — opens dispatch popup directly at worker selection or colony confirm, skipping the destination list

## 2026-02-21 ~11:28–13:00 EST

- Fixed remaining 4 IDLE_AT_DESTINATION transitions in simulation.gd — added mission.workers.clear() after worker-freeing loops (mining done, fuel stop abort, _complete_deploy, _complete_collection)
- Added ore sale transaction logging to market_tab.gd — _sell_all_ores() and _sell_ore() now call record_transaction() (auto-sell at colonies was already logged)
- Investigated "orphan worker in mining unit" autotest error — traced through fire_worker, deploy_mining_unit, _complete_deploy, _process_worker_leave; root cause NOT YET FOUND (see handoff)
- Explained --dangerously-skip-permissions flag and how to set defaultMode in ~/.claude/settings.json

## 2026-02-21 ~10:00–11:28 EST

- Introduced WORK_LOG.txt as a standard end-of-session doc (replaces GIT_LOG.txt)
- Retroactively populated work log with full project history, organized as time ranges
- Updated MEMORY.md and CLAUDE_HANDOFF.md to include WORK_LOG.txt in the update-docs routine
- "update docs" established as the end-of-session command to update WORK_LOG, HANDOFF, and GDD
- Diagnosed false positive leak: ContractsList fills to ~48 nodes (bounded by data caps), not an actual leak
- Fixed leak detector GameState logging: Engine.has_singleton() doesn't work for autoloads, changed to is_instance_valid(GameState)
- Verified worker personality traits fully implemented across all 6 files
- Tick batching fix confirmed: EventBus.tick now emitted once per frame instead of once per simulation step (~30x overhead reduction at SPEED_MAX)
- financial_history trimming changed from slice() to remove_at(0) to avoid allocation
- Ship outfitting tab horizontal expansion finally fixed: clip_text = true on all labels + refresh guard against stacked call_deferred

## 2026-02-21 ~08:00–10:00 EST

- CRITICAL FIX: Food consumption was 100x too fast — unit conversion bug (1 unit = 100kg, not 1kg)
- CRITICAL FIX: Ship teleportation — docked_at_colony not cleared on Earth return caused ships to snap back to old colony
- Auto-provisioning: ships now automatically purchase food when docking at colonies (30-day buffer)
- Test harness: provisions ships before dispatch, mission redirects increased (15%→30% mining, 20% trade added), ships queue next job while busy
- Comprehensive code review vs GDD: multiple features marked "not implemented" were actually fully functional
- Mining units, cargo volume, mining slots, ore stockpiles all confirmed complete
- GDD updated to v0.7: accurate status for mining units, cargo volume, Phase 2b roadmap (8 features marked DONE)
- IMPLEMENTATION_STATUS.md written documenting GDD claims vs actual codebase reality

## 2026-02-20 ~16:00–17:00 EST

- Worker XP system: pilot_xp, engineer_xp, mining_xp fields; quadratic XP curve; skills cap at 2.0
- XP accumulates in simulation: pilot XP during transit, mining XP during extraction, engineer XP on repairs
- Wage auto-scales on level-up; small loyalty bonus per level
- XP progress bars in Workers tab, level-up notifications in dashboard activity feed
- worker_skill_leveled signal added
- Worker personality traits: AGGRESSIVE, CAUTIOUS, LOYAL, GREEDY, LEADER
- Each type modifies mining output, fatigue rate, accident rate, quit chance, tardiness rate
- Greedy workers demand wage raises every 30 game-days if loyalty < 60
- Leader aura: +5% crew mining, -5% crew fatigue for non-Leaders
- Personality saves/loads with LOYAL as default for old saves
- Mining units: full end-to-end system — purchase, deploy, passive ore accumulation, stockpiles, collect, durability degradation, all save/load
- LeakDetector autoload created (F6 overlay, writes to leak_log.txt)
- Test harness created for automated gameplay testing at max speed
- Dashboard tab major expansion: contracts ticker, stationed ships panel, discipline panel, collapsible sections, balance history, reputation, company policies, activity feed, alerts

## 2026-02-20 ~06:43–07:44 EST

- Physics-based rescue system: derelict ships maintain velocity, intercept trajectory, life support tracking
- Complete save/load: missions, trade missions, contracts, market events, fabrication queue, reputation, rescue/refuel missions
- Ship purchasing backend: pricing for all 4 classes, purchase_ship(), ship spawns at Earth fully fueled
- Ship purchasing UI: popup showing all 4 classes with specs and prices, color-coded affordability
- Automated fuel stop routing: FuelRoutePlanner utility, greedy nearest-colony algorithm, waypoint metadata on missions, dispatch UI preview
- CRITICAL FIX: Life support not reset on rescue — ships died instantly on second breakdown
- Engineer self-repair: crew can patch breakdowns in-place before full derelict (0–50% by skill)
- Space key toggles between 1x and previous speed
- docs/ folder created; CLAUDE_HANDOFF.md, GDD.md, LORE.md moved there

## 2026-02-19 ~19:37–22:13 EST

- CRT shader disabled (every pixel every frame)
- Starfield throttled (1000–3000+ circles/frame)
- Patched conics trajectory visualization: replaced forward simulation with analytical conic math (KSP-style, 10–100x faster)
- Orbital updates, label overlap detection, date display, dashboard ticks all throttled to wall-clock intervals
- Sun-only gravity for drifting derelicts
- Ships targeting stale Earth positions fixed, jerkiness at high speed fixed
- CLAUDE_HANDOFF.md created for multi-machine coordination
- Design conversation: server stack (Python/PostgreSQL), policy system, two-tier alerts, colony tiers, consortia, light-speed delay, piracy balance
- GDD v0.5: design pillars, communication delay, policy system, alert system, colony tiers, HQ mechanics, consortia sections all written

## 2026-02-18 ~16:53–21:25 EST

- GDD first full draft (~780 lines)
- Reputation system: model, score, tiers
- Ephemeris verifier: validates positions against JPL data
- Worker model: home colony, loyalty, leave status, fatigue
- JPL ephemeris data integrated into celestial positions
- Solar map: ship markers, orbit rendering, label overlap detection
- Market: random walk pricing, buy/sell UI
- Dashboard: missions, resources, workers, alerts
- Trajectory visualization experiments (forward simulation approach, later replaced by patched conics)
- Trade missions: buy/sell at colonies, revenue calculation
- Ship outfitting tab: equipment upgrades, mining unit management
- Dashboard: financial history, transaction log

## 2026-02-18 ~07:28–07:45 EST

- JPL ephemeris data: real orbital elements for all planets and major bodies
- Colony positions tied to orbital mechanics
- HTTP fetcher service for ephemeris
- Solar map orbit path rendering
- TimeScale autoload: speed control (1x through max), game clock formatting
- Speed controls in main UI

## 2026-02-17 ~19:29–22:42 EST

- Contracts: offer/accept/complete/fail, deadlines, rewards
- Market events: random price shocks
- Ship upgrades: model, catalog, equipment slots
- Brachistochrone trajectory calculation
- Solar map: ship markers, orbit visualization
- TimeScale autoload
- Fleet tab: ship list, status, basic dispatch
- Market tab: prices, buy/sell
- All 4 ship classes defined: Courier, Prospector, Explorer, Hauler (thrust/cargo/fuel specs)
- Fuel pricing: colony-based costs, distance scaling
- Company policy system: thrust strategy
- Trade mission model: full buy/sell flow
- Fleet dispatch UI: crew selection, route preview, fuel cost
- Solar map: ship movement along trajectory
- LORE.md created

## 2026-02-16 ~18:42–21:42 EST

- Project created in Godot 4
- Core autoloads: EventBus, GameState, Simulation
- Core models: Ship, Worker, Mission, Equipment, Colony, Contract, TradeMission
- Core data: AsteroidData, CelestialData, MarketData, ResourceTypes
- Market tab, Fleet tab, Workers tab, Dashboard tab initial builds
- Solar map skeleton
- Brachistochrone physics stub

## 2026-02-21 (time untracked)

- Created `server/` directory with full Python/FastAPI server implementation
- SQLAlchemy 2.0 async + PostgreSQL (asyncpg) + Alembic migrations configured
- ORM models: Player, Ship, Worker, Mission, Asteroid, Colony
- Auth: bcrypt password hashing, JWT tokens, OAuth2 Bearer scheme
- REST routers: /auth (register/login/me), /game (state/dispatch/hire/fire/buy-ship/policies/asteroids/colonies/market), /events (SSE stream), /admin (status/seed/give-starter-pack)
- Simulation background asyncio task: missions (transit/mining/return), market random walk, payroll deduction
- In-memory EventBus for SSE push to all connected clients
- Seed data: 20 real asteroids with Keplerian orbital elements, 9 colonies with price multipliers
- Standalone seed.py script: creates tables, seeds data, creates default player1 with Prospector ship + 3 workers
- README.md with setup instructions and full API reference


## 2026-02-25 ~[time untracked] EST (HK-47)

- Ship Partnership System (complete implementation)
  - Leader/follower model: two ships travel together as coordinated pair
  - Shadow missions: follower syncs with leader (status, position, timing)
  - Mutual aid: automatic fuel transfer and engineer repair for troubled partners
  - Combat bonuses: combined firepower, proportional damage splitting
  - NPC integration: aggressive corps form partnerships for contested asteroids
  - Station support: partnered ships dispatch together, return together
  - Full save/load persistence with name-based reference resolution
  - UI: Fleet tab partnership controls, Dashboard activity log integration
  - Created docs/PARTNERSHIP_SYSTEM.md and partnership_test.gd
  - ~800-1000 lines across 7 core files

